**Removed the confique-driven static path: one schema model, hard removal** ([#104](https://github.com/arthur-debert/clapfig/issues/104)) — `#[derive(clapfig::Schema)]` + `Clapfig::schema_builder::<C>()` is now THE typed path; the parallel confique-backed path (`Clapfig::builder::<C>()`, `ClapfigBuilder<C>`, `Resolver<C>`) is gone, along with the `confique` dependency itself. Every feature that carried a "static-path asterisk" now behaves uniformly: JSON Schema emits `type` on every leaf and `enum` sets; `config gen` runs through the runtime emitter with `# Allowed:` and placeholder hints; post-merge type errors name the offending key; `ArrayOf`/`MapOf` containers and schema-inline strictness work across every consumer of the schema model (templates, JSON Schema, strict validation). One derive-side gap remains: `#[derive(clapfig::Schema)]` does not yet emit `Field::ArrayOf` for `Vec<NestedStruct>` fields ([#106](https://github.com/arthur-debert/clapfig/issues/106); see the migration note below). The `derive` Cargo feature is now a default feature.

**Migration from `#[derive(confique::Config)]`:**

- On the struct: `#[derive(confique::Config, ...)]` → `#[derive(clapfig::Schema, serde::Serialize, serde::Deserialize, ...)]`. The derive does not generate a serde `Deserialize` impl — derive it yourself.
- Attributes: `#[config(default = ..., env = ...)]` → `#[clapfig(default = ..., env = ...)]`. Drop `#[config(nested)]` — a field whose type also derives `Schema` nests automatically. `#[config(deserialize_with = f)]` → serde's `#[serde(deserialize_with = "f")]` (note: unlike confique, schema defaults also pass through the deserializer, so write defaults in pre-normalized form or accept the normalization).
- Builder: `Clapfig::builder::<C>()` → `Clapfig::schema_builder::<C>()`. Same surface (`app_name`, `search_paths`, `env_prefix`, `strict*`, `cli_override*`, `post_validate`, `load`, `load_with_unknowns`, `handle*`).
- Errors: `ClapfigError::ConfigError(confique::Error)` is removed. Missing required fields now surface as `ClapfigError::MissingRequired { key }` and type mismatches as `ClapfigError::InvalidValue { key, .. }` with the exact offending key.
- Removed API without a direct replacement: `Resolver<C>` (typed tree-walk resolver) — use `Clapfig::runtime(C::schema_arc().as_ref().clone()).build_resolver()` (or the runtime builder directly) and deserialize the per-leaf `toml::Table`; a typed `SchemaConfigBuilder::build_resolver` is a candidate follow-up. Also removed: `meta::doc_for::<C>` (use `meta::doc_for_runtime(C::schema(), key)`), `schema::generate_schema::<C>()` (use `schema::generate_schema(C::schema())`), `ops::generate_template::<C>` / `get_value` / `list_values` / `get_scope_value`, and `persist::set_in_document::<C>` / `persist_value::<C>` (runtime twins `set_in_document_runtime` / `persist_value_runtime` remain).
- Field naming: `#[serde(rename = "...")]` on a field is honored by the schema (the schema follows serde's spelling, so no paired attribute is needed); a `#[clapfig(rename)]`/`#[serde(rename)]` pair must match — a differing pair is a derive-time error. Struct-level `rename_all` (clapfig or serde spelling) is rejected at derive time — schema field names are not rewritten through rename rules yet ([#107](https://github.com/arthur-debert/clapfig/issues/107)); rename fields individually. (`rename_all` on unit-only *enums* is fully supported.)
- `Vec<NestedStruct>` fields: not yet derivable as a first-class array-of-tables (`Field::ArrayOf` emission is [#106](https://github.com/arthur-debert/clapfig/issues/106)); the derive rejects them with a pointer to the workaround. Use `#[clapfig(value)]`, which treats the field as an opaque leaf deserialized by serde — the same fidelity confique gave such fields (it had no schema for them either) — or build the schema via the runtime path.
- Confique features with no clapfig equivalent (`#[config(partial_attr(...))]`, `#[config(validate = ...)]`): use serde attributes on the struct and `post_validate` respectively.
